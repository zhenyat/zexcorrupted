<div class='container'>
  <table class='table table-striped'>
    <tbody>
      <% @dotcom.attributes.each do |key, value| %>
        <tr>
          <% unless key == 'id' || key.include?('digest') || key == 'created_at' || key == 'updated_at' %>
            <% if key == 'status' %>
              <td><%= t 'status.status' %></td>
              <td><%= status_mark @dotcom.status %></td>
            <% else %>
              <td><%= t "activerecord.attributes.dotcom.#{key}" %></td>
              <td><%= value %></td>
            <% end %>
          <% end %>
        </tr>
      <% end %>
    </tbody>
  </table>
  <%= render 'admin/shared/show_rich_text_content', object: @dotcom %>
  <%= render 'admin/shared/show_images', object: @dotcom %>
</div>
